cloud/.env
cloud/.env.*
!cloud/.env.example
cloud/_dryrun/
output/
output_cloud/
*.indd.lock
.DS_Store

# --- Public repo: exclude internal brand assets, marketing copy,
#     and the handoff doc. Supply these out-of-band for real cloud runs. ---
assets/
input/
template/
HANDOFF.md
scripts/_extracted_specs.json
# Internal ops/status doc (org ids, colleague names, internal channels) — keep local.
cloud/AEM-EXTENSION-DESIGN.md

# Local-only debranded render helper (hardcoded personal paths)
run_debrand.sh

# Internal follow-ups (real technical-account id + personal Azure container) — keep local.
TODO.md

# App Builder extension: source and deployment manifests are tracked.
# Its nested .gitignore excludes credentials, local environment files, caches,
# dependencies, generated bundles, and deployment output.
